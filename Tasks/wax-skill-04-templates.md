Prompt:
You are the Implementation Agent. Create templates under `templates/` for common Wax tasks.

Goal:
Provide reusable task patterns: initialization, remember/recall, hybrid search, maintenance, and VideoRAG flows.

Task Breakdown:
- Template: initialize store and embedder.
- Template: remember/recall lifecycle (including flush/close if required).
- Template: hybrid search usage.
- Template: maintenance tasks (optimize, compact, flush/close).
- Template: VideoRAG ingest/recall with transcripts.

Expected Output:
A set of task template files under `templates/`, each with concise steps and placeholders for user-specific values.
