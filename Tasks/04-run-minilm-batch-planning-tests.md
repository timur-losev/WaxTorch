Prompt:
Run the targeted Swift Testing suite for MiniLM batch planning.

Goal:
Validate the new SPI hook, tests, and clamping logic by executing the focused test filter.

Task BreakDown:
- From repo root, run: `swift test --filter MiniLMEmbedderBatchPlanningTests`.
- Confirm the test target passes; capture any failures for follow-up.
