Prompt:
You are the Implementation Agent. Add concise Swift 6.2 examples to `SKILL.md`.

Goal:
Provide accurate, minimal examples for MemoryOrchestrator and VideoRAGOrchestrator usage.

Task Breakdown:
- Example: initialize and remember.
- Example: recall and handle RAGContext.
- Example: error handling with `do/catch`.
- Example: VideoRAG ingest and recall with transcript requirement.

Expected Output:
Swift code blocks in `SKILL.md` aligned to public APIs, with no speculative calls.
